export function crewReducer(state = {}, action) {

	switch (action.type) {

		default: return state
	}
}


export function storageReducer(state = {}, action) {

	switch (action.type) {

		default: return state
	}
}

export function healthReducer(state = {}, action) {

	switch (action.type) {

		default: return state
	}
}
